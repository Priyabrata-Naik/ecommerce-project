#jwt:
#  secretKey: dgheiqp350cndpeuie8324qsiecnceiwdiepacdcpedeidpccdqajfioejrfojeojqowejofjoejofj

spring:
  application:
    name: api-gateway

  config:
    import: configserver:http://config-server:8888
  cloud:
    config:
      fail-fast: false
      retry:
        initial-interval: 2000
        max-attempts: 6
        max-interval: 10000

#  #api gateway config
#  cloud:
#    gateway:
#      server:
#        webflux:
#          routes:
#            - id: order-service
#              uri: lb://ORDER-SERVICE
#              predicates:
#                - Path=/api/v1/orders/**
#              filters:
#                - StripPrefix=2
#                - name: LoggingOrdersFilter
#                - name: Authentication
#                  args:
#                    isEnabled: true
##                - RedirectTo=302, http://codingshuttle.com
##               - name: Authorization
##                 args:
##                   allowedRoles: ADMIN,SUPER_ADMIN
#            - id: inventory-service
#              uri: lb://INVENTORY-SERVICE
#              predicates:
#                - Path=/api/v1/inventory/**
#              filters:
#                - StripPrefix=2
#                - AddRequestHeader=X-Custom-Header, Shark
#                - name: Authentication
#
#
#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:8761/eureka